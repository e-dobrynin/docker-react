language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t e-dobrynin/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true e-dobrynin/docker-react npm run test
